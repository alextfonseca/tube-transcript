import { buttonToken } from "@/design-tokens/components/button";
import { inputToken } from "@/design-tokens/components/input";

export const theme = {
  components: {
    Button: buttonToken,
    Input: inputToken,
  },

  token: {
    colorPrimary: "#16a34a",
  },
};
